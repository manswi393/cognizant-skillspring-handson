using System;

class Forecast
{
    public static double Predict(double value, int years, double rate)
    {
        if (years == 0)
            return value;

        return Predict(value * (1 + rate), years - 1, rate);
    }
}